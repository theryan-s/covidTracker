import React, { Component } from 'react';
import { Line, Bar } from 'react-chartjs-2';


<select id="dropdown">

    for (country in countriesArray) {
        const newOption = `<option>{$country.Country}</option>`;
        #dropdown.append(newOption);
    }

</select>